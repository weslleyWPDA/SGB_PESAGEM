<input required class=" upper" type="{{ $type }}" name="{{ $name }}" placeholder="{{ $place }}"
    autocomplete="off" value="{{ $value ?? null }}"
    style="width:300px;
            color: rgb(0,0,0);font-size: 15px;text-align: left;
            height: 30px;border-radius: 0px; margin:5px 0 5px 5px;padding: 7px">
