<button class="btn btn-primary botoes" type="{{ $type }}"
    style="background-color:{{ $color }};width: {{ $width ?? 'auto' }};
    margin: 5px;border:none;font-weight: 800;">
    {{ $label }}
</button>
